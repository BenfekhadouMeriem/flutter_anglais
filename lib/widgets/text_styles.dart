import 'package:flutter/material.dart';

class AppTextStyles {
  // Nouveau fontWeight pour appName
  static const TextStyle appName = TextStyle(
    fontSize: 26,
    fontWeight: FontWeight.w400, // Changer ici selon le poids désiré
    color: Colors.white,
  );

  // Nouveau fontWeight pour welcomeText
  static const TextStyle welcomeText = TextStyle(
    fontSize: 28,
    fontWeight: FontWeight.w400, // Changer ici selon le poids désiré
    color: Colors.pink,
  );

  // Nouveau fontWeight pour buttonText
  static const TextStyle buttonText = TextStyle(
    fontSize: 18,
    fontWeight: FontWeight.w500, // Changer ici selon le poids désiré
    color: Colors.white,
  );

  // Nouveau fontWeight pour outlinedButtonText
  static const TextStyle outlinedButtonText = TextStyle(
    fontSize: 18,
    fontWeight: FontWeight.w500, // Changer ici selon le poids désiré
    color: Colors.pink,
  );
}
